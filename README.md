# Anna's Nest 🪺 — Per Anna 🕊️ + Lago di Anna 🌸 + the Mixtape 📼

One GitHub Pages site. `index.html` is the nest — the homepage that links to
everything else — and each gift lives behind one of its doors:

0. **`index.html` — the nest.** The landing page: three tiles (the letter, the
   game, the mixtape) plus a dashed slot for whatever gets built next. Plain
   self-contained HTML + inline SVG; the only external request is Google Fonts
   (Cormorant Garamond + Jost). Adding a gift = copy a `<a class="tile">` block,
   set its `--accent` / `--wash` colors, `href`, heading, and blurb.
1. **`letter.html` — the letter.** An airmail envelope she flips and unseals,
   then a scrolling story (the mixer, the 1 AM message, coffee at Kinship, the
   globe, the photo wall, the birthday note, the countdown). The "Press to play"
   ticket at the end links to the game at `./game/`.
2. **`game/index.html` — the game.** A tiny Stardew-style Lake Como world.
   Anna's pixel character explores the map, plays mini-games hidden in
   different spots, and unlocks a letter at the end.
3. **`cassette/index.html` — the mixtape.** A cassette deck with ten songs
   (five a side). Each J-card unfolds into the note for that track; the copy
   lives in `cassette/notes.txt` and the track data in `cassette/tape.json`.

## Deploy to GitHub Pages

1. Push this folder to a GitHub repo (`index.html` at the repo root).
2. Repo → Settings → Pages → Source: `Deploy from a branch`, branch `main`, folder `/ (root)`.
3. Wait a minute, then open `https://<username>.github.io/<repo>/` — that opens
   the nest, and every gift is one click from there.

To preview locally, run `python3 -m http.server` in the repo root and open
`http://localhost:8000/` (opening `index.html` as a file works too, but the
`./game/` ticket link needs a server).

## Project structure

```
index.html         ← the nest / homepage (plain, self-contained HTML + inline SVG)
annas-nest.html    ← original claude.ai export of the nest — REFERENCE ONLY:
                     a bundled design-canvas file with editor placeholders.
                     index.html is the converted, working copy; make text
                     edits in index.html.
letter.html        ← the letter (plain, self-contained HTML+JS)
Per Anna.dc.html   ← original claude.ai export of the letter — REFERENCE ONLY:
                     it needs a support.js runtime that isn't here, so it does
                     not run by itself. letter.html is the converted, working
                     copy; make text edits in letter.html.
cassette/index.html ← the mixtape player (bundled, self-contained)
photos/            ← photo-wall images (anna-01.png … anna-16.png) — SEE BELOW
game/index.html    ← the built game (generated by build.py — don't hand-edit)
build.py           ← assembles game/index.html from the template + games + assets
games/             ← one file per mini-game, editable individually:
                       family_tree.js      (Sphynx family tree — canvas)
                       find_the_dog.js     (flashlight hide-and-seek — canvas)
                       squirrel_nest.js    (fluff-catching at the old oak — canvas)
                       platypus-creek.html (standalone page, embedded whole at the dock)
                       ranunculus.html     (standalone turning-bloom puzzle, embedded
                                            whole at the garden; reports solves to the
                                            world via postMessage "ranunculus-solved")
assets/            ← processed game sprites (transparent bg, game-scaled) + map
```

## TODO — images the letter still needs

The letter auto-hides any frame whose image is missing, so the page never
shows broken images — but these should be added:

- `photos/anna-01.png` … `photos/anna-16.png` — the polaroid photo wall
- `anna-avatar.png` (repo root) — Anna's avatar in the chat mock
- `perry.jpg` (repo root) — the platypus plushie polaroid near the signature

Dates baked into the letter (edit in `letter.html` if plans change):
countdown target `2026-08-20T19:00:00-04:00` (search for it), the
"August 20 · 7:00 PM" label, and the envelope's "do not open until July 25".

## Rebuilding the game

`build.py` contains the world/engine HTML/CSS/JS template as a Python string;
the mini-games live in `games/*.js` and are spliced in at the `__GAMES__`
marker (new games: add the file and list it in `GAME_FILES` in `build.py`).
After editing either, run:

```
python3 build.py
```

which re-embeds the assets and regenerates `game/index.html`.

## Game TODO / where things live in build.py

- **The Fruit Stall** holds the finished thank-you letter: once every game
  is done, talking to the stall opens `#fletter` (the `fruitstand_letter.png`
  banner + the full message). The letter text lives in the `#fletter` div in
  `build.py`. (The old draft finale letter at the dock has been removed —
  the boat is purely ambient now.)
- **All in-game text is finalized** — no draft/placeholder copy remains. The
  four games are the family tree, ranunculus, find-the-dog, and squirrel nest,
  plus the Platypus Creek arcade at the dock. Anna's House and the Fruit Stall
  are flavor spots, not games.
- **Persistence:** progress is in-memory (`progress` object). On GitHub Pages,
  localStorage is available — serialize `progress` + `mapFlowers` there.
- **Map coordinates:** collision polygons/rects are at the top of the JS
  (`lakePoly`, `solids`, `dockRect`, `zones`). The map is 1369×1149; coordinates
  are in map pixels.

## Game architecture notes

- One `<canvas>` renders the world with a cover-fit camera that follows Anna.
- All ambient creatures (dogs, platypus, boat, cat) share a tiny state-machine
  pattern (`pause / walk / follow / hop`, plus dive for the platypus).
- Mini-games are objects (`famGame`, `dogGame`, `squirrelGame`)
  with `init/layout/down/move/up/update/draw`, rendered on a separate overlay
  canvas (`#gcvs`). `openGame(obj)` / `closeGame()` switch between world and
  game. An optional `intro` string (+ `introBtn` label) shows a start screen
  with instructions; input and updates stay paused until the player starts.
  All games are replayable from their zone dialogs after completion.
- Depth sorting: land sprites are drawn sorted by feet-Y each frame.
- Waypoint signs (`waypoints` array + `drawMarker()` in `build.py`) float over
  every game spot with the game's name so the player can see where to go; two
  more follow the dogs. A finished game's sign fades to a ✓, and the fruit-stall
  sign flips to "a letter for you" once everything is complete.
- Family-tree photos come from `assets/cat_<name>.{png,jpg,jpeg,webp}`
  (malva, lola, sergei, naomi, funtik) — already present; drop in a new file
  and rebuild to swap one.
